{
  "name": "mindkeeper-monorepo",
  "private": true,
  "description": "Time machine for your AI's brain — version control for agent context files",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "pnpm@10.6.2",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "lint": "oxlint .",
    "clean": "pnpm -r run clean"
  }
}
